{
  "name": "organization-ui",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "main": "src/server.js",
  "scripts": {
    "build": "webpack -p",
    "server": "NODE_ENV=prod babel-node --presets env ./src/server.js",
    "server-mon": "NODE_ENV=dev nodemon --exec babel-node --presets env ./src/server.js",
    "start": "npm run build; npm run server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.5.3",
    "cookie-parser": "~1.4.4",
    "express": "^4.16.4",
    "mongoose": "^5.5.14",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-loader": "^7.1.5",
    "babel-preset-env": "^1.7.0",
    "nodemon": "^1.19.1",
    "webpack": "^3.12.0"
  }
}
